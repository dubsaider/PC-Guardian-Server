# Архив старых миграций

Эта директория содержит старые миграции, которые уже были выполнены и больше не используются в активной разработке.

## Миграции

- `migrate_add_peripherals.py` - Добавление полей `agent_version` и `peripherals` (выполнена)
- `migrate_add_system_info.py` - Добавление поля `system_info` (выполнена)

Эти миграции сохранены для истории и справки, но не должны выполняться повторно.







