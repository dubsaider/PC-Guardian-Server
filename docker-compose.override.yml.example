# Пример файла для переопределения настроек в docker-compose.yml
# Скопируйте этот файл в docker-compose.override.yml и настройте под свои нужды

services:
  kafka:
    environment:
      # IP адрес хоста для внешнего доступа к Kafka (если нужно переопределить значение по умолчанию)
      # По умолчанию используется: 10.53.16.49
      # Узнать IP: Windows: ipconfig, Linux/Mac: hostname -I или ifconfig
      KAFKA_ADVERTISED_HOST: 10.53.16.49
  
  server:
    environment:
      # Использование PostgreSQL вместо SQLite
      # DATABASE_URL: postgresql://user:password@postgres:5432/pcguardian
      
      # Настройки Telegram
      TELEGRAM_BOT_TOKEN: your_telegram_bot_token
      TELEGRAM_CHAT_ID: your_telegram_chat_id
      
      # Настройки Email
      SMTP_HOST: smtp.gmail.com
      SMTP_PORT: 587
      SMTP_USER: your_email@gmail.com
      SMTP_PASSWORD: your_app_password
      EMAIL_FROM: your_email@gmail.com
      EMAIL_TO: admin@example.com,admin2@example.com

  # Раскомментируйте для использования PostgreSQL
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: pc-guardian-postgres
  #   environment:
  #     POSTGRES_USER: pcguardian
  #     POSTGRES_PASSWORD: pcguardian_password
  #     POSTGRES_DB: pcguardian
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #   networks:
  #     - pc-guardian-network
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U pcguardian"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

# volumes:
#   postgres-data:

